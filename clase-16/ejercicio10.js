const prompt = require("prompt-sync")();
// ejercicio 10 construir una frase a partir de palabras

let palabras = ['Me', 'gusta', 'aprender', 'JavaScript'];

// join une todo en un solo string con espacios
let frase = palabras.join(' ');

console.log(frase);